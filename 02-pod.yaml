# apiVersion: v1
# kind: Pod

# metadata:
#    name: nginx
#    namespace: roboshop
#    labels: 
#       name: nginx
#       project: roboshop
# spec:
#    containers: 
#    - name: nginx 
#      image: nginx
#      ports:
#      - containerPort: 80


apiVersion: v1 
kind: Pod

metadata:
   name: nginx
   namespace: roboshop
   labels:
     environment: development
      project: robosohop
   spec:
   containers:
   - name: nginx
     image: nginx
     ports:
       containerPort: 80
